---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: clusterrequests.oran.openshift.io
spec:
  group: oran.openshift.io
  names:
    kind: ClusterRequest
    listKind: ClusterRequestList
    plural: clusterrequests
    singular: clusterrequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterRequest is the Schema for the clusterrequests API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRequestSpec defines the desired state of ClusterRequest
            properties:
              clusterTemplateInput:
                description: JSON Schema input used for the clusterTemplateRef.
                type: string
              clusterTemplateRef:
                description: Reference to an existing clusterTemplate CR.
                type: string
            required:
            - clusterTemplateInput
            - clusterTemplateRef
            type: object
          status:
            description: ClusterRequestStatus defines the observed state of ClusterRequest
            properties:
              clusterTemplateInputValidation:
                description: Tells if the schema provided in spec.clusterTemplateInput
                  is a valid JSON.
                properties:
                  clusterTemplateInputError:
                    description: Holds the error in case the ClusterTemplateInput
                      is invalid.
                    type: string
                  clusterTemplateInputIsValid:
                    description: Says of the ClusterTemplateInput is valid or not.
                    type: boolean
                required:
                - clusterTemplateInputIsValid
                type: object
            required:
            - clusterTemplateInputValidation
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
