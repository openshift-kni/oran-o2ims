---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: hardwareplugins.o2ims-hardwaremanagement.oran.openshift.io
spec:
  group: o2ims-hardwaremanagement.oran.openshift.io
  names:
    kind: HardwarePlugin
    listKind: HardwarePluginList
    plural: hardwareplugins
    singular: hardwareplugin
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HardwarePlugin is the Schema for the hardwareplugins API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HardwarePluginSpec defines the desired state of HardwarePlugin
            properties:
              apiRoot:
                description: ApiRoot is the root URL for the Hardware Plugin.
                minLength: 1
                type: string
            required:
            - apiRoot
            type: object
          status:
            description: HardwarePluginStatus defines the observed state of HardwarePlugin
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
