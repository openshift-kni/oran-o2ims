---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: clustertemplates.oran.openshift.io
spec:
  group: oran.openshift.io
  names:
    kind: ClusterTemplate
    listKind: ClusterTemplateList
    plural: clustertemplates
    singular: clustertemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterTemplate is the Schema for the clustertemplates API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterTemplateSpec defines the desired state of ClusterTemplate
            properties:
              inputDataSchema:
                description: JSON formatted string that includes the schema for the
                  ClusterTemplate. Required values should be explicitly defined.
                type: string
            required:
            - inputDataSchema
            type: object
          status:
            description: ClusterTemplateStatus defines the observed state of ClusterTemplate
            properties:
              clusterTemplateValidation:
                description: Tells if the schema provided in spec.inputDataSchema
                  is a valid JSON.
                properties:
                  clusterTemplateError:
                    description: Holds the error in case the ClusterTemplate is invalid.
                    type: string
                  clusterTemplateIsValid:
                    description: Says of the ClusterTemplate is valid or not.
                    type: boolean
                required:
                - clusterTemplateIsValid
                type: object
            required:
            - clusterTemplateValidation
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
