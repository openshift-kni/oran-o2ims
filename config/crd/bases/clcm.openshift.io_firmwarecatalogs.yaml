---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: firmwarecatalogs.clcm.openshift.io
spec:
  group: clcm.openshift.io
  names:
    kind: FirmwareCatalog
    listKind: FirmwareCatalogList
    plural: firmwarecatalogs
    shortNames:
    - fwcatalog
    - fwcatalogs
    singular: firmwarecatalog
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The age of the FirmwareCatalog resource.
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.conditions[-1:].reason
      name: Reason
      type: string
    - jsonPath: .status.conditions[-1:].status
      name: Status
      type: string
    - jsonPath: .status.conditions[-1:].message
      name: Details
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwareCatalog is the Schema for the firmwarecatalogs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirmwareCatalogSpec defines the desired state of FirmwareCatalog.
            properties:
              images:
                description: Images is the set of firmware images available in this
                  catalog.
                items:
                  description: FirmwareImage defines a firmware image entry in the
                    catalog.
                  properties:
                    component:
                      description: Component identifies the hardware component type.
                      enum:
                      - bios
                      - bmc
                      - nic
                      type: string
                    description:
                      description: Description is an optional human-readable description
                        of the firmware image.
                      type: string
                    name:
                      description: Name is a unique identifier for this firmware image
                        within the catalog.
                      minLength: 1
                      type: string
                    url:
                      description: URL points to the firmware image file.
                      minLength: 1
                      pattern: ^(http|https)://.*$
                      type: string
                    vendor:
                      description: Vendor identifies the firmware vendor or manufacturer.
                      minLength: 1
                      type: string
                    version:
                      description: Version is the firmware version string.
                      minLength: 1
                      type: string
                  required:
                  - component
                  - name
                  - url
                  - version
                  type: object
                  x-kubernetes-validations:
                  - message: component is immutable
                    rule: self.component == oldSelf.component
                  - message: url is immutable
                    rule: self.url == oldSelf.url
                  - message: version is immutable
                    rule: self.version == oldSelf.version
                  - message: vendor is immutable
                    rule: self.vendor == oldSelf.vendor
                maxItems: 1024
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
          status:
            description: FirmwareCatalogStatus defines the observed state of FirmwareCatalog.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              imageStatuses:
                description: ImageStatuses reports the validation result for each
                  image in the catalog.
                items:
                  description: ImageValidationStatus reports the validation result
                    for a single image entry.
                  properties:
                    message:
                      description: Message provides a human-readable description of
                        the validation result.
                      type: string
                    name:
                      description: Name is the name of the firmware image entry.
                      type: string
                    reason:
                      description: Reason provides a machine-readable reason for the
                        validation result.
                      type: string
                    valid:
                      description: Valid indicates whether the image entry passed
                        validation.
                      type: boolean
                  required:
                  - name
                  - valid
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
