# This configuration teaches kustomize to apply namePrefix to
# ServiceAccount references in ClusterRole resourceNames fields.
nameReference:
- kind: ServiceAccount
  version: v1
  fieldSpecs:
  - kind: ClusterRole
    group: rbac.authorization.k8s.io
    path: rules/resourceNames
